const Driver_dashboard = (props) => {
    return(
        <div>
            
            <h1>Driver Dashboard</h1>
            < Driver_active_trip />
            < Driver_future_trip />
            < Driver_past_trip />
        </div>
    )
}